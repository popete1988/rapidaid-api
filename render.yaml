services:
  - type: web
    name: rapidaid-api
    runtime: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/rapidaid-api-1.0.0.jar
    envVars:
      - key: PORT
        value: 8080
    plan: free
